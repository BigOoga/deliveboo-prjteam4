<?php

use Illuminate\Database\Seeder;

class ReviewSeeder extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {        
        // //NUMERO DI RECENSIONI
        // $total_review = 10;

        // // RANDOMMIZZO UN NUMERO DI RECENSIONI
        // $randomReviewNum = rand(1, 20);

        // //CICLO FOR PER STAMPA RECENSIONI
        // $randomReviewIds = [];
        // for ($i = 0; $i < $randomReviewNum; $i++) {
        //     $randomReviewId = rand(1, $total_review);
        //     $randomReviewIds[] = $randomReviewId;
        // };

        // $review->vote()->attach($randomReviewIds);
    }
}
